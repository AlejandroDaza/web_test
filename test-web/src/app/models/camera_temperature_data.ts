import { DecimalPipe } from "@angular/common"

export interface Camera_Temperature_Data{
    id?: number
    camera_id?: number
    temperature_id?: number
    value?: number
    observation_date?: Date
}